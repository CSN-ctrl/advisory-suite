# =============================================================================
# Single source of truth: copy to ".env" at the repo root (never commit .env).
# Vercel: use the same variable names under Settings → Environment Variables.
# =============================================================================

# --- Local dev servers ---
# Port for `npm run dev` (Vite). Default 8080 if unset.
VITE_DEV_PORT=8080
# Port for `npm run api` (Express). Default 8787 if unset.
API_PORT=8787

# --- Supabase (Dashboard → Settings → API) ---
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_PUBLISHABLE_KEY=

# Vite / browser (never put service_role here).
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=
VITE_SUPABASE_PUBLISHABLE_KEY=

# --- Admin (plain password; no hash) ---
# Remove ADMIN_PASSWORD_HASH from Vercel/.env when using plain password.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=
# Optional cookie signing secret; else derived from SUPABASE_SERVICE_ROLE_KEY.
# ADMIN_SESSION_SECRET=

# Advanced only — bcrypt; if set, ADMIN_PASSWORD is ignored.
# ADMIN_PASSWORD_HASH=